import preloader from './../../../assets/images/1474.gif';

let Preloader = (props) => {
  return (
    <div>
      <img src={preloader} alt={'aa'}></img>
    </div>
  );
};

export default Preloader;
